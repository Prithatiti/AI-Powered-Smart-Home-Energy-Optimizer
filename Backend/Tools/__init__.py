"""
Agent tools for the Smart Home Energy Optimizer.

Each tool is a deterministic function that agents can call to retrieve data
or perform a specific operation.  Tools live in this package; shared
helper services (e.g. geocoding) live under ``Backend/Tools/services/``.
"""

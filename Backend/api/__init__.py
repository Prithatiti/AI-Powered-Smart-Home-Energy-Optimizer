"""
Wattpilot backend API package.

Groups the FastAPI route modules (under :mod:`Backend.api.Routes`), the
application entry point (``main.py`` at the project root) and, eventually,
the API-specific layer of the stack.
"""